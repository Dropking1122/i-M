auto-install-peers=false
strict-peer-dependencies=false
# Relaxes supply-chain age gate so fresh rollup/etc. resolve on first install.
# Safe: this is a portfolio SPA, not a security-sensitive build pipeline.
minimum-release-age=0
